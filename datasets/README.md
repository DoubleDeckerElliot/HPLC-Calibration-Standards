# Datasets

Place sample and fixture datasets for HPLC integration here.

Expected format:
- Comma-separated text
- No header row
- First column = x/time
- Second column = y/signal

Examples can be used by tests in `../tests/`.
